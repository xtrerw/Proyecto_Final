import React from 'react'

const ScrollPaginas = (props) => {
  return (
    <main style={{
        backgroundImage:`url(${props.img})`,
    }}>
        
    </main>
  )
}

export default ScrollPaginas
